const data = [
  { title: '', desc: '' },
  { title: '', desc: '' },
  { title: '', desc: '' },
  { title: '', desc: '' },
  { title: '', desc: '' },
];

const selected = ref('');

// const res = data.filter(({ title, desc }) => title === selected.value || desc === selected.value);

//
// const res2 = data.filter(({ title }) => title.length).map(({ title }) => title);
